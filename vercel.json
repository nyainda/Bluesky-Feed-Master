{
  "buildCommand": "pnpm --filter @workspace/bluesky-feeds run build",
  "outputDirectory": "artifacts/bluesky-feeds/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
